# A题专用 Prompt Q4：收缩移动边界烘干时间

```text
$math-modeling-code-experiment

只解决Q4。读取附件2、附录4、Q4冻结的移动域模型、坐标/模板编码决定和长时环境规则。任何一项未冻结则BLOCKED。

权威数据/公式：附件2给t=0..259200 s、每1800 s的半径R(t)，2.000 cm降至1.198 cm；ρ=760+90C；cp=1850+2150·C/(C+1)；k=0.12+0.20·C/(C+1)；D=4.2×10^-4·exp(-0.30/C)·exp(-3850/T)，T用K。不得沿用附录3参数。

执行：
1. 按冻结插值构造R(t)，在原始节点精确回代且不产生非物理增大/过冲；
2. 在移动物理域或等价变换域上求解，正确处理网格速度/雅可比/表面边界；轴向收缩或质量守恒规则只能来自MODEL_CONTRACT；
3. 求首次满足整个当前药材域内C<0.15的t_end，使用连续事件定位；
4. 与“固定半径但使用附录4物性”baseline比较，分离收缩与物性变化贡献；
5. 做R(t)插值、Δr/移动网格分辨率、Δt和长时边界敏感性；核验总质量、正性、边界和固定R退化测试；
6. Q4_RESULT.json保存t_end、限制位置（物理cm及必要的归一化坐标）、每6 h/0.5 cm至表面的论文表；超过当时表面的坐标不得伪填数值；
7. 复制模板为11_submission/result4.xlsx，严格按已冻结的矩形编码输出每60 s、每0.1 cm及“药材表面”值；不得自行把固定坐标、归一化坐标或空值规则混用；
8. 回读Excel，验证每行有效空间域、表面列、单位、四位小数和JSON一致性。

输出：06_questions/Q4/...、Q4_RESULT.json、Q4_SOLUTION_NOTES.md、11_submission/result4.xlsx。

硬验收：R(t)回代/单调PASS；移动域守恒PASS；阈值首次穿越PASS；网格收敛PASS；result4编码与回读PASS。完成后停止。
```

